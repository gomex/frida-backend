{
  "name": "frida-backend",
  "description": "Small service to perform CRUD operations for news",
  "main": "index.js",
  "private": true,
  "license": "GPLv2",
  "dependencies": {
    "async": "^1.5.0",
    "basic-auth": "^1.0.3",
    "body-parser": "^1.13.3",
    "compression": "^1.5.2",
    "express": "~4.0.0",
    "express-winston": "^0.4.1",
    "gray-matter": "^2.0.1",
    "js-yaml": "^3.4.3",
    "mkdirp": "^0.5.1",
    "moment": "^2.10.6",
    "mongodb": "^2.0.40",
    "slug": "^0.9.1",
    "underscore": "^1.8.3",
    "winston": "^1.0.1"
  },
  "engines": {
    "node": "~0.12.5"
  },
  "scripts": {
    "start": "node ./index.js",
    "test": "export NODE_TLS_REJECT_UNAUTHORIZED='0'; export KEY_FILE_PATH=./test/specs/ssl/ssl.key; export CERT_FILE_PATH=./test/specs/ssl/ssl.crt; ./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly --  test/specs --recursive -R spec --timeout 10000 && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "debug": "node --debug-brk ./index.js",
    "jshint": "./node_modules/jshint/bin/jshint ."
  },
  "devDependencies": {
    "coveralls": "^2.11.4",
    "istanbul": "^0.4.0",
    "jshint": "^2.8.0",
    "mocha": "^2.2.5",
    "protractor": "^2.2.0",
    "supertest": "^1.0.1"
  }
}
